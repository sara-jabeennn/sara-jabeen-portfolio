This file exists only so Turbopack's dynamic-import static analysis for
`app/blog/[slug]/page.tsx` has at least one `.mdx` file to resolve against -
an empty `content/blog/` directory fails the build outright. Excluded from
`getBlogSlugs()` by its leading underscore. Delete this the moment a real
post exists.
